# 已知问题 / 待办

> 记录测试中发现但暂未修复的问题，供后续处理。

## 语音相关

### ISSUE-1：超时停止时音频为空（待复现/修复）
- **现象**：录音超过 10s 被自动停止时，`识别耗时 0.05s` 且结果为"没听清"。
- **对比**：手动按停（录音 2-3s）时识别正常（7-9s）。
- **推测**：超时路径（`_auto_stop`）与录音停止存在竞态，或超时时音频缓冲未收到数据。
- **状态**：暂不修（用户要求先记录）。

### ISSUE-2：STT 识别偶有掉词
- **现象**：说"诺里斯的圈速比我快多少"，被识别为"我和圈速比我快多少"（丢了"诺里斯"）。
- **影响**：AI 拿到不通顺的问题，只能瞎猜。
- **可能原因**：small 模型对专有名词（车手名）识别弱；或录音音质。
- **状态**：暂不处理（用户要求）。

### ISSUE-3：STT 识别耗时偏慢
- **数据**：2-3s 音频 → 识别 7.7-9.0s（与游戏同时运行时）。
- **单独 bench**：4s 音频 → 5.9s。
- **推测**：与游戏抢 CPU。
- **可选优化**：换 base 模型 / 上 GPU（需装 CUDA 库）/ faster-whisper 参数。
- **状态**：暂不处理。

## AI / 数据相关

### ISSUE-4：领先/落后的方向可能反了（待核实）
- **现象**：用户当时在**落后**状态，但 AI 回答"你**领先**诺里斯 2.4 秒"。
- **疑问**：是 `gap_to_leader` / 排行榜数据的语义问题，还是 AI 理解错误？
- **需核实**：
  - 当时的实际名次（用户 P几、诺里斯 P几）
  - `leaderboard` 里 `gap_to_leader_ms` 的方向定义（正 = 落后领先者？）
  - 提示词里对 gap 的表述是否会让 AI 读反
- **状态**：待核实，暂不修。

### ISSUE-5：AI 回答偶尔啰嗦
- **现象**：回答里附加解释（如"现在正对前面的车发起攻击"）。
- **处理**：通过提示词调整（用户说之后改）。
- **状态**：待办（提示词）。

## 待整理

- 语音模块（`voice_*.py`）尚未提交到 GitHub。
- `stt_lib/`（260MB+ 依赖）和 `stt_models/`（460MB+）不应提交，需在 `.gitignore` 排除。
- `test_stt_mic.py` / `bench_stt.py` / `bench_gpu.py` / `voice_runner.py` 等测试脚本是否保留待定。
